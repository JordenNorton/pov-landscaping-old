import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#4caf50', // Green shade for landscaping theme
        },
        secondary: {
            main: '#ff9800', // Complementary color
        },
    },
});

export default theme;
